# Changelog

このファイルでは、pdf2png の主な変更点を記録します。

## Unreleased

### Added

- GUIを追加。引数なしで起動すると、PDFファイルと出力先フォルダを選択して変換できるようになりました。
- GUIで既存ZIPがある場合の上書き確認を追加。
- GUI上に変換中の進捗ログ、完了ダイアログ、エラーダイアログを追加。
- 出力先指定と既存ZIP検出の回帰テストを追加。

### Changed

- CLIの変換処理を共通関数へ整理し、GUIとCLIで同じ変換ロジックを使うようにしました。
- READMEと設計/実装計画ドキュメントをGUI化後の仕様に合わせて更新。

### Notes

- CLI実行時の挙動は従来通りです。`pdf2png <file.pdf>` ではPDFと同じフォルダにZIPを出力し、同名ZIPがある場合は上書きします。
