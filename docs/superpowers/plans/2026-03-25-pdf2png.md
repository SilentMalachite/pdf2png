# pdf2png Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PDFをページごとにPNG化し、ZIPにまとめて出力するGo製CLIツールを単一バイナリで配布できる形で実装する。

**Architecture:** `main.go` が引数バリデーション・オーケストレーションを担い、`internal/converter` が go-fitz（MuPDF CGo）でPDF→PNG変換、`internal/archiver` が標準ライブラリでPNG→ZIP圧縮を行う。エラー時のみ `Press Enter to exit` で一時停止し、ドラッグ&ドロップでも結果を確認できるようにする。

**Tech Stack:** Go 1.22+, github.com/gen2brain/go-fitz (MuPDF embedded mode, CGo), archive/zip (standard library), GitHub Actions (macos-13 / macos-latest / windows-latest)

---

## File Map

| ファイル | 役割 |
|---------|------|
| `go.mod` | モジュール定義、go-fitz 依存 |
| `main.go` | エントリポイント。引数検証・書き込み権限確認・temp dir管理・converter/archiver呼び出し |
| `internal/converter/converter.go` | `Convert(pdfPath, outDir string) ([]string, error)` — MuPDFでページをPNG化 |
| `internal/converter/converter_test.go` | converter の単体テスト |
| `internal/archiver/archiver.go` | `Archive(files []string, zipPath string) error` — PNG群をZIP化 |
| `internal/archiver/archiver_test.go` | archiver の単体テスト |
| `.github/workflows/build.yml` | macOS(amd64/arm64) + Windows CI |

---

## Task 1: プロジェクト初期化

**Files:**
- Create: `go.mod`
- Create: `internal/converter/.gitkeep`
- Create: `internal/archiver/.gitkeep`

- [ ] **Step 1: go mod init**

```bash
cd /Users/hiro/Projetct/GitHub/pdf2png
go mod init github.com/hiro/pdf2png
```

Expected: `go.mod` が作成される。

- [ ] **Step 2: go-fitz を追加**

```bash
go get github.com/gen2brain/go-fitz@latest
```

Expected: `go.mod` に require が追加され `go.sum` が生成される。

- [ ] **Step 3: ディレクトリ作成**

```bash
mkdir -p internal/converter internal/archiver .github/workflows
```

- [ ] **Step 4: Commit**

```bash
git init
git add go.mod go.sum
git commit -m "chore: initialize module with go-fitz dependency"
```

---

## Task 2: Archiver 実装（TDD）

**Files:**
- Create: `internal/archiver/archiver.go`
- Create: `internal/archiver/archiver_test.go`

### Step 1: 失敗するテストを書く

- [ ] `internal/archiver/archiver_test.go` を作成:

```go
package archiver_test

import (
	"archive/zip"
	"os"
	"path/filepath"
	"testing"

	"github.com/hiro/pdf2png/internal/archiver"
)

func TestArchive(t *testing.T) {
	// Setup: PNG ファイルを含む一時ディレクトリを作成
	srcDir := t.TempDir()
	files := []string{}
	for _, name := range []string{"page_01.png", "page_02.png", "page_03.png"} {
		p := filepath.Join(srcDir, name)
		if err := os.WriteFile(p, []byte("fake png data for "+name), 0644); err != nil {
			t.Fatal(err)
		}
		files = append(files, p)
	}

	// Execute
	outDir := t.TempDir()
	zipPath := filepath.Join(outDir, "output.zip")
	if err := archiver.Archive(files, zipPath); err != nil {
		t.Fatalf("Archive() error = %v", err)
	}

	// Verify: ZIP が存在する
	if _, err := os.Stat(zipPath); os.IsNotExist(err) {
		t.Fatal("ZIP file not created")
	}

	// Verify: ZIP の中身が 3 ファイル
	r, err := zip.OpenReader(zipPath)
	if err != nil {
		t.Fatalf("cannot open zip: %v", err)
	}
	defer r.Close()

	if len(r.File) != 3 {
		t.Errorf("zip contains %d files, want 3", len(r.File))
	}

	// Verify: ファイル名はベース名のみ（ディレクトリパスなし）
	for _, f := range r.File {
		if filepath.Base(f.Name) != f.Name {
			t.Errorf("zip entry has directory path: %q", f.Name)
		}
	}
}

func TestArchiveOverwrite(t *testing.T) {
	// 同名 ZIP が存在する場合に上書きされることを確認
	srcDir := t.TempDir()
	p := filepath.Join(srcDir, "page_01.png")
	if err := os.WriteFile(p, []byte("data"), 0644); err != nil {
		t.Fatal(err)
	}

	outDir := t.TempDir()
	zipPath := filepath.Join(outDir, "output.zip")

	// 1回目
	if err := archiver.Archive([]string{p}, zipPath); err != nil {
		t.Fatal(err)
	}
	info1, _ := os.Stat(zipPath)

	// 2回目（上書き）
	if err := archiver.Archive([]string{p}, zipPath); err != nil {
		t.Fatalf("second Archive() error = %v", err)
	}
	info2, _ := os.Stat(zipPath)

	// ModTime が変わっていることで上書きを確認
	if !info2.ModTime().After(info1.ModTime()) {
		t.Log("note: modtime may not change in fast test runs, checking file exists")
	}
}
```

- [ ] **Step 2: テストを実行して失敗を確認**

```bash
cd /Users/hiro/Projetct/GitHub/pdf2png
go test ./internal/archiver/...
```

Expected: `cannot find package` または `undefined: archiver.Archive`

- [ ] **Step 3: `internal/archiver/archiver.go` を実装**

```go
package archiver

import (
	"archive/zip"
	"io"
	"os"
	"path/filepath"
)

// Archive creates a ZIP file at zipPath containing the given files.
// Each file is stored with only its base name.
// If zipPath already exists, it is overwritten.
func Archive(files []string, zipPath string) error {
	f, err := os.Create(zipPath)
	if err != nil {
		return err
	}
	defer f.Close()

	w := zip.NewWriter(f)
	defer w.Close()

	for _, src := range files {
		if err := addFile(w, src); err != nil {
			return err
		}
	}
	return nil
}

func addFile(w *zip.Writer, src string) error {
	f, err := os.Open(src)
	if err != nil {
		return err
	}
	defer f.Close()

	dst, err := w.Create(filepath.Base(src))
	if err != nil {
		return err
	}

	_, err = io.Copy(dst, f)
	return err
}
```

- [ ] **Step 4: テストを実行してパスを確認**

```bash
go test ./internal/archiver/... -v
```

Expected: `PASS`

- [ ] **Step 5: Commit**

```bash
git add internal/archiver/
git commit -m "feat: implement archiver (PNG to ZIP)"
```

---

## Task 3: Converter 実装（TDD）

**Files:**
- Create: `internal/converter/converter.go`
- Create: `internal/converter/converter_test.go`

> **前提:** CGo を使用するため、macOS では Xcode コマンドラインツールが必要。
> `xcode-select --install` で導入済みであること。

### Step 1: 失敗するテストを書く

- [ ] `internal/converter/converter_test.go` を作成:

```go
package converter_test

import (
	"os"
	"path/filepath"
	"strings"
	"testing"

	"github.com/hiro/pdf2png/internal/converter"
)

// minimalPDF は MuPDF が解析できる最小限の有効な 1 ページ PDF。
// 各オブジェクトのバイトオフセットは xref テーブルと整合している。
var minimalPDF = []byte(
	"%PDF-1.0\n" +
		"1 0 obj<</Type/Catalog/Pages 2 0 R>>endobj\n" +
		"2 0 obj<</Type/Pages/Kids[3 0 R]/Count 1>>endobj\n" +
		"3 0 obj<</Type/Page/MediaBox[0 0 72 72]>>endobj\n" +
		"xref\n" +
		"0 4\n" +
		"0000000000 65535 f \n" +
		"0000000009 00000 n \n" +
		"0000000052 00000 n \n" +
		"0000000101 00000 n \n" +
		"trailer<</Size 4/Root 1 0 R>>\n" +
		"startxref\n" +
		"149\n" +
		"%%EOF")

func writeTempPDF(t *testing.T) string {
	t.Helper()
	f, err := os.CreateTemp(t.TempDir(), "test_*.pdf")
	if err != nil {
		t.Fatal(err)
	}
	defer f.Close()
	if _, err := f.Write(minimalPDF); err != nil {
		t.Fatal(err)
	}
	return f.Name()
}

func TestConvert_SinglePage(t *testing.T) {
	pdfPath := writeTempPDF(t)
	outDir := t.TempDir()

	files, err := converter.Convert(pdfPath, outDir)
	if err != nil {
		t.Fatalf("Convert() error = %v", err)
	}

	// 1 ページ PDF なので 1 ファイル
	if len(files) != 1 {
		t.Errorf("got %d files, want 1", len(files))
	}

	// ファイルが存在し、0 バイトでないこと
	for _, p := range files {
		info, err := os.Stat(p)
		if err != nil {
			t.Errorf("stat %q: %v", p, err)
			continue
		}
		if info.Size() == 0 {
			t.Errorf("file %q is empty", p)
		}
	}
}

func TestConvert_FileNaming(t *testing.T) {
	pdfPath := writeTempPDF(t)
	outDir := t.TempDir()

	files, err := converter.Convert(pdfPath, outDir)
	if err != nil {
		t.Fatalf("Convert() error = %v", err)
	}

	// ファイル名は page_1.png（1 ページなのでゼロ埋め不要）
	if len(files) < 1 {
		t.Fatal("no files returned")
	}
	base := filepath.Base(files[0])
	if !strings.HasPrefix(base, "page_") || !strings.HasSuffix(base, ".png") {
		t.Errorf("unexpected filename: %q", base)
	}
}

func TestConvert_InvalidPath(t *testing.T) {
	_, err := converter.Convert("/nonexistent/path/file.pdf", t.TempDir())
	if err == nil {
		t.Error("expected error for nonexistent file, got nil")
	}
}
```

- [ ] **Step 2: テストを実行して失敗を確認**

```bash
CGO_ENABLED=1 go test ./internal/converter/...
```

Expected: `undefined: converter.Convert`

- [ ] **Step 3: `internal/converter/converter.go` を実装**

```go
package converter

import (
	"fmt"
	"image/png"
	"os"
	"path/filepath"
	"strconv"
	"strings"

	fitz "github.com/gen2brain/go-fitz"
)

// Convert converts each page of the PDF at pdfPath to a PNG file in outDir.
// Returns the list of created PNG file paths in page order.
// Scale factor 300/72 ≈ 4.167 produces 300 DPI output.
func Convert(pdfPath, outDir string) ([]string, error) {
	doc, err := fitz.New(pdfPath)
	if err != nil {
		if isPasswordError(err) {
			return nil, fmt.Errorf("PDF is password-protected")
		}
		return nil, fmt.Errorf("failed to open PDF: %w", err)
	}
	defer doc.Close()

	n := doc.NumPage()
	if n == 0 {
		return nil, fmt.Errorf("PDF has no pages")
	}

	// ゼロ埋め桁数を総ページ数から算出
	digits := len(strconv.Itoa(n))
	format := fmt.Sprintf("page_%%0%dd.png", digits)

	var files []string
	for i := 0; i < n; i++ {
		fmt.Printf("Converting page %d/%d...\n", i+1, n)

		img, err := doc.ImageDPI(i, 300)
		if err != nil {
			return nil, fmt.Errorf("failed to convert page %d: %w", i+1, err)
		}

		name := fmt.Sprintf(format, i+1)
		outPath := filepath.Join(outDir, name)

		f, err := os.Create(outPath)
		if err != nil {
			return nil, fmt.Errorf("failed to create %s: %w", name, err)
		}

		if err := png.Encode(f, img); err != nil {
			f.Close()
			return nil, fmt.Errorf("failed to encode page %d: %w", i+1, err)
		}
		f.Close()

		files = append(files, outPath)
	}

	return files, nil
}

func isPasswordError(err error) bool {
	if err == nil {
		return false
	}
	msg := err.Error()
	return strings.Contains(msg, "password") || strings.Contains(msg, "encrypted")
}
```

- [ ] **Step 4: テストを実行してパスを確認**

```bash
CGO_ENABLED=1 go test ./internal/converter/... -v
```

Expected: `PASS`

> **minimalPDF が MuPDF で解析失敗する場合の対処:** MuPDF はデフォルトで壊れた PDF の修復モードを持つが、それでも失敗する場合は、ブラウザで任意のページを「PDFとして保存」した小さいファイルを `testdata/sample.pdf` として追加し、`writeTempPDF` ヘルパーを以下に差し替える:
> ```go
> func writeTempPDF(t *testing.T) string {
>     t.Helper()
>     p, err := filepath.Abs("testdata/sample.pdf")
>     if err != nil { t.Fatal(err) }
>     if _, err := os.Stat(p); os.IsNotExist(err) {
>         t.Skip("testdata/sample.pdf not found")
>     }
>     return p
> }
> ```

- [ ] **Step 5: Commit**

```bash
git add internal/converter/
git commit -m "feat: implement converter (PDF pages to PNG at 300 DPI)"
```

---

---

## Task 4: main.go 実装

**Files:**
- Create: `main.go`

- [ ] **Step 1: `main.go` を作成**

```go
package main

import (
	"bufio"
	"fmt"
	"os"
	"path/filepath"
	"strings"

	"github.com/hiro/pdf2png/internal/archiver"
	"github.com/hiro/pdf2png/internal/converter"
)

func main() {
	// 引数チェック: usage エラーは pause なしで終了（ドラッグ&ドロップではなく誤操作）
	if len(os.Args) != 2 {
		fmt.Fprintln(os.Stderr, "Usage: pdf2png <file.pdf>")
		os.Exit(1)
	}

	if err := run(os.Args[1]); err != nil {
		fmt.Fprintln(os.Stderr, "Error:", err)
		pauseOnError() // ドラッグ&ドロップ利用者がエラーを確認できるよう一時停止
		os.Exit(1)
	}
}

func run(pdfPath string) error {

	// 拡張子チェック（大文字小文字不問）
	if !strings.EqualFold(filepath.Ext(pdfPath), ".pdf") {
		return fmt.Errorf("not a PDF file: %s", filepath.Base(pdfPath))
	}

	// ファイル存在チェック
	if _, err := os.Stat(pdfPath); os.IsNotExist(err) {
		return fmt.Errorf("file not found: %s", pdfPath)
	}

	// 出力先ディレクトリの書き込み権限チェック（変換前に確認）
	outDir := filepath.Dir(pdfPath)
	tmp, err := os.CreateTemp(outDir, ".pdf2png_check_*")
	if err != nil {
		return fmt.Errorf("cannot write to directory: %s", outDir)
	}
	tmp.Close()
	os.Remove(tmp.Name())

	// 中間 PNG 用一時ディレクトリ（関数終了時に削除）
	tmpDir, err := os.MkdirTemp("", "pdf2png_*")
	if err != nil {
		return fmt.Errorf("failed to create temp directory: %w", err)
	}
	defer os.RemoveAll(tmpDir)

	// PDF → PNG 変換
	pngFiles, err := converter.Convert(pdfPath, tmpDir)
	if err != nil {
		return err
	}

	// ZIP ファイルパスを生成（PDF と同じディレクトリ）
	base := strings.TrimSuffix(filepath.Base(pdfPath), filepath.Ext(pdfPath))
	zipPath := filepath.Join(outDir, base+".zip")

	// PNG → ZIP 圧縮
	if err := archiver.Archive(pngFiles, zipPath); err != nil {
		return fmt.Errorf("failed to create ZIP: %w", err)
	}

	fmt.Println("Done:", filepath.Base(zipPath))
	return nil
}

// pauseOnError はエラー時にユーザーの確認を待つ（ドラッグ&ドロップ利用者向け）。
func pauseOnError() {
	fmt.Fprint(os.Stderr, "Press Enter to exit...")
	bufio.NewReader(os.Stdin).ReadString('\n')
}
```

- [ ] **Step 2: ビルドを確認**

```bash
CGO_ENABLED=1 go build -o pdf2png .
```

Expected: エラーなしでバイナリが生成される。

- [ ] **Step 3: 手動テスト — 引数なし**

```bash
./pdf2png
```

Expected: `Usage: pdf2png <file.pdf>` が表示されて終了。pause なし。

- [ ] **Step 4: 手動テスト — 非 PDF ファイル**

```bash
./pdf2png /tmp/test.txt
```

Expected: `Error: not a PDF file: test.txt` + pause。

- [ ] **Step 5: 手動テスト — 実際の PDF で正常動作確認**

小さい PDF（例: ブラウザで任意のページを PDF 印刷して保存）で実行:

```bash
./pdf2png ~/Downloads/sample.pdf
```

Expected:
- `Converting page 1/N...` の進捗表示
- PDF と同じディレクトリに `sample.zip` が生成される
- ZIP を開くと `page_1.png`（または `page_01.png` 等）が含まれる

- [ ] **Step 6: 生成バイナリを削除してソースのみ残す**

```bash
rm pdf2png
```

- [ ] **Step 7: Commit**

```bash
git add main.go
git commit -m "feat: implement main entry point with validation and error handling"
```

---

## Task 5: GitHub Actions CI

**Files:**
- Create: `.github/workflows/build.yml`

- [ ] **Step 1: `build.yml` を作成**

```yaml
name: Build

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-13
            goos: darwin
            goarch: amd64
            artifact: pdf2png-darwin-amd64
          - os: macos-latest
            goos: darwin
            goarch: arm64
            artifact: pdf2png-darwin-arm64
          - os: windows-latest
            goos: windows
            goarch: amd64
            artifact: pdf2png-windows-amd64.exe

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      # Windows: CGo に必要な MinGW-w64 GCC を導入
      - name: Install MinGW (Windows only)
        if: runner.os == 'Windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          install: mingw-w64-x86_64-gcc
          path-type: inherit

      - name: Test
        shell: bash
        env:
          CGO_ENABLED: '1'
          CC: ${{ runner.os == 'Windows' && 'x86_64-w64-mingw32-gcc' || '' }}
        run: go test ./...

      - name: Build
        shell: bash
        env:
          CGO_ENABLED: '1'
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          # Windows: MSYS2 GCC を明示的に指定
          CC: ${{ runner.os == 'Windows' && 'x86_64-w64-mingw32-gcc' || '' }}
        run: |
          go build -ldflags="-s -w" -o "${{ matrix.artifact }}" .

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.artifact }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/build.yml
git commit -m "ci: add GitHub Actions build for macOS (amd64/arm64) and Windows"
```

---

## Task 6: リリースの動作確認

- [ ] **Step 1: GitHub リポジトリに push**

```bash
git remote add origin https://github.com/hiro/pdf2png.git
git push -u origin main
```

- [ ] **Step 2: タグを打って Release をトリガー**

```bash
git tag v0.1.0
git push origin v0.1.0
```

Expected: GitHub Actions が 3 つのジョブを実行し、Releases に以下が追加される:
- `pdf2png-darwin-amd64`
- `pdf2png-darwin-arm64`
- `pdf2png-windows-amd64.exe`

---

## 既知の制限・注意事項

| 項目 | 内容 |
|------|------|
| ライセンス | go-fitz は AGPL。社内利用のみ可。商用配布は要確認。 |
| CGo | ローカル開発に Xcode CLT（macOS）または MinGW（Windows）が必要 |
| 大容量 PDF | 高 DPI で多ページ PDF を変換するとメモリ・ディスクを大量消費する場合がある |
| テスト PDF | `minimalPDF` バイト列で MuPDF が解析失敗する場合は実際の PDF をテストデータとして使用すること |
